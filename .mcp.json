{
  "mcpServers": {
    "playwright": {
      "command": "npx",
      "args": [
        "-y",
        "@playwright/mcp@latest",
        "--isolated",
        "--blocked-origins",
        "localhost;127.0.0.1;192.168.*;10.*"
      ]
    }
  }
}
